"""AI services module (Phase 2 - the provider abstraction is defined now).

Default provider is OpenRouter; Groq is supported for free-tier usage. A mock
provider lets the system run without any API key. See ``provider.py``.

TODO(phase-2): persist ai_requests history and expose the assistant router.
"""
